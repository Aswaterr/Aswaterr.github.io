/* ============================================================
   CONFIGURATION SUPABASE — colle ici les valeurs de ton projet
   ============================================================
   Où les trouver : supabase.com/dashboard > ton projet >
   icône ⚙️ Project Settings > API.
   - "Project URL"       -> SUPABASE_URL
   - "anon public" key    -> SUPABASE_ANON_KEY

   Tant que ces valeurs ne sont pas remplies, l'appli fonctionne
   quand même, mais uniquement en local (pas de synchro entre
   appareils).
   ============================================================ */

window.OSCILLO_SUPABASE_CONFIG = {
  url: "COLLE_TA_VALEUR_ICI",
  anonKey: "COLLE_TA_VALEUR_ICI"
};
